---> printf() function in C language:

	{
		In C programming language, printf() function is used to print the (“character, string, float, integer, octal and hexadecimal values”) onto the output screen.
    		: %d to display the value of an integer variable.
    		: %ld to display long integer
    		: %c is used to display character 
    		: %f for float variable
				0.5 to set precision to 5 for 
    				right hand value from dot(.)
    		: %s for string variable
    		: %lf for double  -->
    				0.5 to set precision to 5 for 
    				right hand value from dot(.)
    		: %x for hexadecimal variable.
    		: %o fro octal value.
    	}
--> Array of Object :
	{
		class <obj-name>[] = {}; or
		class <obj-name>[<n>];
		<obj-name>[<i>] = new <obj>;
		

